#tuple is data structure similar to a list except the fact that it is immutable.
#this means that there can be no changes made inside a tuple
#the elements inside a tuple are enclosed within ().

mytuple=(5,8)
print(mytuple[0])
